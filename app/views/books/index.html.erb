<% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: name %>
    <% end %>
    
    
    
    
    <h1> Books </h1>
    <% @books.each do |book| %>
    <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>Body</th>
        </tr>
      </thead>
      <tbody>
       <tr>
         <td></td>
         <td></td>
       </tr>
      </tbody>





    </table>


<span class="book.title"><%= book.title %></span>

<div class="Body">
<span class="book.body"><%= book.body %></span>



<%= link_to "Show",book_path(book.id) %>  <%= link_to "Edit",edit_book_path(book.id) %>  <%= link_to "Destroy",books_path(book.id),method: :delete,"data-confirm" => "Are you sure?" %>

<% end %>



<!--ーーーーーーーーーここまでーーーーーーーーーーーーーー-->


<h2>New book</h2>
<% if @book.errors.any? %>
 <%= @book.errors.count %>errors prohibited this book from being saved:
 <ul>
   <% @book.errors.full_messages.each do |message| %>
   <li><%= message %></li>
   <% end %>
 </ul>
<% end %>


<%= form_with model:@book,url:'/books',method: :post do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>
 <br>
  <%= f.submit 'Create Book' %>
<% end %>
